import React, {PropTypes} from 'react';

export default class AnimalTable extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (<div>AnimalTable</div>);
  }
}

AnimalTable.propTypes = {
};
